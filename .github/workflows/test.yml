# Generated by ConfigOps UI. Edits made here are overwritten on the next push.
name: Run OpKey CLI

on:
  workflow_dispatch:

jobs:
  run-cli:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Run OpKey Task
        run: >
          java -jar "OpKeyCLI-0.6-jar-with-dependencies.jar"
          --hostname "http://localhost:5207"
          --username "${{ secrets.OPKEY_USERNAME }}"
          --apikey "${{ secrets.OPKEY_API_KEY }}"
          --project "Opkey"
          --module devopstask
          --command createApplyconfigurationTask
          --task-name "ApplyConfigurations_2026_09_16 15_29_49_063"
          --agent-id "387c7323-c075-458b-a89f-f26ae1387ffd"
          --requirement-id "71b9a0a1-9864-4106-8a98-f158a04dd5e3"
          --parent-id "00000000-0000-0000-0000-000000000000"
          --environment-id "71b9a0a1-9864-4106-8a98-f158a04dd5e3"
          --module-name "Financials"
          --sub-module "General Ledger"
          --task-files "FND_CREATE_IMPLEMENTATION_USERS"
          --reporter-id "eb38792e-3180-41ea-ac00-017646064392"
